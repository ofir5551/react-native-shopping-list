# Roadmap

## Planned Features

### Shared List Indicators
- In the lists screen, visually indicate which lists are shared (e.g. a share icon or badge).
- Inside a shared list, show a subtle sync indicator to communicate that the list is synced to the cloud via Supabase.
- Private (non-shared) lists should show no sync indicator — they live only in local storage.
